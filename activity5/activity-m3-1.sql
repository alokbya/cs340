-- select the columns ProductNumber, ProductName and ListPrice
-- and sort by ListPrice in descending order (most expensive first)
SELECT ProductNumber, ProductName, ListPrice
FROM Products
ORDER BY ListPrice DESC;
